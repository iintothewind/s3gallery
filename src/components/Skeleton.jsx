import React from "react";

/** Animated shimmer placeholder shown while thumbnails are loading. */
export default function Skeleton() {
  return <div className="thumbnail skeleton-tile" aria-hidden="true" />;
}
